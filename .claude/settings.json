{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run --silent contract -- --summary 2>/dev/null || echo 'Contract check could not run (is Node installed? see CLAUDE.md).'"
          }
        ]
      }
    ]
  }
}
